#include "gtest/gtest.h"
#include "../Run/CollisionHandler.h"

using namespace std;

TEST(CollisionHandler, Instance) {
  EXPECT_NO_THROW({
    //CollisionHandler collisionHandler = CollisionHandler();
  });
}
